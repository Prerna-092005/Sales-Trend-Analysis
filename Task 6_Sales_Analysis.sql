--SALES TREND ANALYSIS USING AGGREGATION
--Creating the Order Table
CREATE TABLE ORDERS(ORDER_ID INT,ORDER_DATE DATE,AMOUNT DECIMAL(10,2),PRODUCT_ID INT); 

--INSERTING SAMPLE DATA
INSERT INTO ORDERS (ORDER_ID, ORDER_DATE, AMOUNT, PRODUCT_ID)VALUES
(1,'2023-01-15', 200.00,101),
(2,'2023-01-20', 150.00,102),
(3,'2023-02-10', 300.00,103),
(4,'2023-02-12', 250.00,104),
(5,'2023-03-05', 200.00,105),
(6,'2023-03-20', 500.00,106),
(7,'2023-03-25', 450.00,107),
(8,'2023-04-15', 100.00,108);

--Monthly Sales Trend Query
SELECT 
EXTRACT(YEAR FROM ORDER_DATE) AS YEAR,
EXTRACT(MONTH FROM ORDER_DATE) AS MONTH,
TO_CHAR(ORDER_DATE, 'MONTH')AS MONtH_NAME,
SUM(AMOUNT) AS MONTHLY_REVENUE,
COUNT(DISTINCT ORDER_ID)AS ORDER_VOLUME
FROM
ORDERS
GROUP BY
EXTRACT(YEAR FROM ORDER_DATE),
EXTRACT(MONTH FROM ORDER_DATE),
TO_CHAR(ORDER_DATE,'MONTH')
ORDER BY
YEAR, MONTH;

--Product Wise Total Sales
SELECT
PRODUCT_ID,
SUM(AMOUNT)AS TOTAL_SALES
FROM
ORDERS
GROUP BY
PRODUCT_ID
ORDER BY
TOTAL_SALES DESC;